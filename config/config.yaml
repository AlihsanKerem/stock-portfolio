"""
# Database Configuration
database:
  type: "sqlite"  # sqlite or postgresql
  sqlite:
    path: "database/portfolio.db"
  postgresql:
    host: "localhost"
    port: 5432
    database: "portfolio_db"
    user: "postgres"
    password: "${DB_PASSWORD}"  # From environment variable

# Market Data
market_data:
  provider: "yfinance"
  update_interval: 3600  # seconds (1 hour)
  retry_attempts: 3
  timeout: 30

# Cash Accounts
cash:
  default_currency: "TRY"
  currencies:
    - "USD"
    - "TRY"
    - "EUR"

# Technical Analysis
analysis:
  indicators:
    rsi_period: 20
    macd_fast: 12
    macd_slow: 26
    macd_signal: 9
    sma_short: 50
    sma_long: 200
  
  signal_thresholds:
    rsi_oversold: 30
    rsi_overbought: 70
    strong_signal: 0.7  # -1.0 to 1.0 scale

# Automation
automation:
  daily_snapshot_time: "18:00"  # UTC time
  price_update_time: "22:00"
  enable_auto_snapshot: true

# Dashboard
dashboard:
  port: 8501
  theme: "dark"
  refresh_interval: 300  # seconds

# Logging
logging:
  level: "INFO"  # DEBUG, INFO, WARNING, ERROR
  file: "logs/portfolio.log"
  max_size: 10485760  # 10MB
  backup_count: 5

# Risk Management
risk:
  max_position_size: 0.20  # Max 20% in single position
  max_sector_allocation: 0.40  # Max 40% in single sector
  stop_loss_default: 0.08  # 8% stop loss
"""